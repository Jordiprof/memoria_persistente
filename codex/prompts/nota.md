Obtén la hora REAL ejecutando `date +%H:%M` (no la inventes). Luego ejecuta el
**RITUAL DE NOTA** de `./AGENTS.md`: añade `- {HORA} — {texto}` al final de `## Notas` del
journal de hoy (`./memory/journal/<HOY>.md`); créalo desde `__KIT_HOME__/templates/journal.md`
si falta. **No** hagas commit.

Texto de la nota: $ARGUMENTS
